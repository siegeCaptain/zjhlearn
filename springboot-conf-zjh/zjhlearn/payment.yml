server:
  port: ${port.payment}

paymentServiceUrl: http://${host.payment}:${port.payment}

zooKeeper:
    hosts: ${zooKeeperHosts}
    project: ${projectName}
    service: ${service.payment}
    port: ${port.payment}
